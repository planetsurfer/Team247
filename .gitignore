.venv/
.env
__pycache__/
output.json
events.jsonl
dashboard.html
agent_v0.md

# Generated per-agent team specs + zip (rebuilt by dashboard.py on every run)
team_specs/
agent_team_specs.zip

# Stand-alone scraper outputs (raw posting text — not committed)
# A small sample fixture under data/fixtures/ IS committed for --dry-run.
data/cache/
data/role_enrichment.json

# Frontend build tooling (built bundle IS committed at app/static/)
web/node_modules/
web/tsconfig.tsbuildinfo

# Design handoff drop (extracted + implemented in web/; archive not tracked)
Simple chatGPT-style frontend (1).zip

# Human-user simulation harness outputs (python -m tests.simulation)
sim_results/

# Production app runtime DB + backups (FastAPI app at app/)
data/agentproof.db
data/agentproof.db-journal
data/agentproof.db-wal
data/agentproof.db-shm
*.bak

# generated Agent Skill scaffolds (regenerate via skill_bundle_service)
data/skill_cache/
